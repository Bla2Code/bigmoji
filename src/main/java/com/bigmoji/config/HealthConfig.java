package com.bigmoji.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class HealthConfig {
  // Spring Boot 4 actuator health contributor APIs changed.
  // Keep configuration placeholder here; health endpoint remains available via actuator defaults.
}
